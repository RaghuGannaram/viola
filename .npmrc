##############################################################
# 📦 NPMRC CONFIGURATION REFERENCE
# Default values are set here.
# Each entry has comments explaining what happens if you flip it.
##############################################################

# ================================
# 1. REGISTRY & AUTHENTICATION
# ================================
registry=https://registry.npmjs.org/             # Default public npm registry.
                                                 # Change per-scope using @scope:registry.
always-auth=false                                # Default: false
                                                 # true → always send credentials (needed for private registries).
                                                 # false → send only when needed.

strict-ssl=true                                  # Default: true
                                                 # true → require valid SSL certs.
                                                 # false → allow self-signed/unverified SSL.

# Auth tokens / credentials (default: none, you must configure if private registry)
# //registry.example.com/:_authToken=${NPM_TOKEN}

# proxy, https-proxy, noproxy (default: none)
# proxy=
# https-proxy=
# noproxy=

# SSL custom certs (default: none)
# cafile=
# ca=
# cert=
# key=

# otp=                                             # Default: none. One-time password for 2FA registries.

# ================================
# 2. DEPENDENCY RESOLUTION & INSTALL
# ================================
legacy-peer-deps=false                           # Default: false
                                                 # true → ignore peer dep conflicts (npm v6 behavior).
                                                 # false → enforce peer dep validation.

strict-peer-deps=false                           # Default: false
                                                 # true → fail install on unresolved peer deps.
                                                 # false → install anyway.

force=false                                      # Default: false
                                                 # true → force re-download deps, ignore cache/lock.
                                                 # false → normal resolution.

prefer-offline=false                             # Default: false
                                                 # true → prefer cache, avoid network.
                                                 # false → normal resolution.

prefer-online=false                              # Default: false
                                                 # true → prefer hitting network even if cache is valid.
                                                 # false → normal resolution.

offline=false                                    # Default: false
                                                 # true → fail if deps require network.
                                                 # false → fetch from registry as needed.

audit=true                                       # Default: true
                                                 # true → run security audit after install.
                                                 # false → skip audit.

audit-level=low                                  # Default: low
                                                 # low → report all severities.
                                                 # moderate/high/critical → filter by severity.

fund=true                                        # Default: true
                                                 # true → show "fund this package" messages.
                                                 # false → suppress.

package-lock=true                                # Default: true
                                                 # true → generate package-lock.json.
                                                 # false → skip lockfile.

package-lock-only=false                          # Default: false
                                                 # true → only update lockfile, don’t install.
                                                 # false → normal install.

shrinkwrap=false                                 # Default: false
                                                 # true → use legacy npm-shrinkwrap.json.
                                                 # false → use modern package-lock.json.

save-exact=false                                 # Default: false
                                                 # true → save dependencies without ^ or ~.
                                                 # false → use prefix.

save-prefix=^                                    # Default: ^
                                                 # ^ → allow minor/patch upgrades.
                                                 # ~ → allow patch upgrades only.
                                                 # (empty) → exact version.

omit=                                            # Default: none
                                                 # Comma-separated: dev,optional,peer
                                                 # Skips installing those dependency types.

include=dev,optional                             # Default: dev,optional
                                                 # Explicitly include dependency types.

engine-strict=true                               # Default: false
                                                 # true → enforce Node/npm versions in package.json engines.
                                                 # false → just warn.

workspaces=true                                  # Default: true (auto-detect)
                                                 # true → enable workspace support in monorepos.

install-strategy=hoisted                         # Default: hoisted
                                                 # hoisted → flatten deps.
                                                 # nested → per-package node_modules.
                                                 # shallow → shallow deps only.

lockfile-version=                                # Default: auto
                                                 # 1/2/3 depending on npm version.
                                                 # Typically do not override.

# ================================
# 3. SCRIPTS & LIFECYCLE
# ================================
ignore-scripts=false                             # Default: false
                                                 # true → skip all lifecycle scripts (postinstall, prepare, etc.).
                                                 # false → run scripts.

scripts-prepend-node-path=auto                   # Default: auto
                                                 # auto → add Node to PATH if mismatch detected.
                                                 # true → always prepend.
                                                 # false → never prepend.

unsafe-perm=false                                # Default: false (true if root)
                                                 # true → allow root scripts to run as root (needed in Docker).
                                                 # false → drop privileges.

foreground-scripts=false                         # Default: false
                                                 # true → stream script output in foreground.
                                                 # false → buffer output.

# node-options=                                  # Default: empty
                                                 # Pass Node CLI flags (e.g. --max-old-space-size=4096).

# ================================
# 4. CACHE & PERFORMANCE
# ================================
cache-min=10                                     # Default: 10 (seconds)
                                                 # Minimum time before npm checks registry again.

cache-max=Infinity                               # Default: Infinity
                                                 # Max staleness allowed before refetch.

prefer-dedupe=false                              # Default: false
                                                 # true → attempt to dedupe dependencies across workspaces.
                                                 # false → normal.

maxsockets=50                                    # Default: 50
                                                 # Max concurrent HTTP requests.

fetch-retries=2                                  # Default: 2
                                                 # Retry attempts on failed fetch.

fetch-retry-factor=10                            # Default: 10
                                                 # Exponential backoff multiplier.

fetch-retry-mintimeout=10000                     # Default: 10000ms (10s)
                                                 # Minimum retry delay.

fetch-retry-maxtimeout=60000                     # Default: 60000ms (60s)
                                                 # Maximum retry delay.

fetch-timeout=300000                             # Default: 300000ms (5min)
                                                 # Per-request timeout.

# ================================
# 5. LOGGING & USER EXPERIENCE
# ================================
loglevel=silly                                   # Default: notice
                                                 # silent/error/warn/notice/http/timing/info/verbose/silly

progress=true                                    # Default: true
                                                 # true → show progress bar.
                                                 # false → disable.

color=auto                                       # Default: auto
                                                 # always → force colors.
                                                 # never → no colors.
                                                 # auto → detect TTY.

# ================================
# 6. PUBLISHING / RELEASE
# ================================
access=public                                    # Default: public
                                                 # public → anyone can install.
                                                 # restricted → only org users.

tag=latest                                       # Default: latest
                                                 # Dist-tag assigned to publishes.

# publish-branch=                                # Default: none
                                                 # Restrict publishing to this branch.

allow-same-version=false                         # Default: false
                                                 # true → allow republishing same version.
                                                 # false → error.

sign-git-tag=false                               # Default: false
                                                 # true → sign git tags with GPG.
                                                 # false → unsigned tags.

git-tag-version=true                             # Default: true
                                                 # true → create git tag on `npm version`.
                                                 # false → skip tagging.

dry-run=false                                    # Default: false
                                                 # true → simulate publish/install.
                                                 # false → actually run.

# ================================
# 7. SECURITY / INTEGRITY
# ================================
ignore-workspace-root-check=false                # Default: false
                                                 # true → allow install in root of monorepo.
                                                 # false → block.

prefer-symlink=false                             # Default: false
                                                 # true → symlink native deps.
                                                 # false → normal copy.

# ================================
# 8. INIT DEFAULTS
# ================================
init-author-name=Raghu Gannaram                  # Default: system username
init-author-email=mail@raghugannaram.com         # Default: empty
init-author-url=https://raghugannaram.com        # Default: empty
init-license=MIT                                 # Default: ISC
init-version=1.0.0                               # Default: 1.0.0